[project]
name = "game-ux-board-local"
version = "0.1.0"
description = "Local Game UX Board server for Codex-assisted installation."
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "opencv-python-headless>=4.13.0.92",
    "pillow>=12.2.0",
]

[tool.uv]
package = false
